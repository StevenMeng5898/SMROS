#[repr(C, align(16))]
#[derive(Clone, Copy, Debug, Default)]
pub struct Aarch64ExceptionFrame {
    pub regs: [u64; 32],
    pub simd: [u128; 32],
    pub fpcr: u64,
    pub fpsr: u64,
}

#[repr(C, align(16))]
#[derive(Clone, Copy, Debug, Default)]
pub struct CpuContext {
    pub x0: u64,
    pub x1: u64,
    pub x2: u64,
    pub x3: u64,
    pub x4: u64,
    pub x5: u64,
    pub x6: u64,
    pub x7: u64,
    pub x8: u64,
    pub x9: u64,
    pub x10: u64,
    pub x11: u64,
    pub x12: u64,
    pub x13: u64,
    pub x14: u64,
    pub x15: u64,
    pub x16: u64,
    pub x17: u64,
    pub x18: u64,
    pub x19: u64,
    pub x20: u64,
    pub x21: u64,
    pub x22: u64,
    pub x23: u64,
    pub x24: u64,
    pub x25: u64,
    pub x26: u64,
    pub x27: u64,
    pub x28: u64,
    pub fp: u64,
    pub lr: u64,
    pub sp: u64,
    pub pc: u64,
    pub pstate: u64,
    pub sp_el0: u64,
    pub elr_el1: u64,
    pub spsr_el1: u64,
    pub tpidr_el0: u64,
    pub ttbr0_el1: u64,
    pub fpcr: u64,
    pub fpsr: u64,
    pub simd: [u128; 32],
}
